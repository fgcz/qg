# TODO


## queue_app.py 
- I think it is time to integrate the queue_generator src/qg/cli/generate_queues.py into the interface.

- changes to interface multiple tabs in the righ panel
- show the created queue in separate tab on the right
- what I also would like to have on the right is a tab allowing to select samples, not allways all samples from an order should be sent into a queue. Maybe there is a way to select multiple rows in a table -> not thinking about checkbox, but rather, clicking on row and adding rows with ctrl - click. 

