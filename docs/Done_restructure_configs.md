# DONE
Motivation : better structure configurations.

Separate queue generator configuration from those which are relevant only for gui.

Some configs, are only relevant for the gui e.g. combinations? 
helps the user to navigate, limit options - but otherwise. not really.

Then is the instrument relevant for the generator? except of output path generation?
instrument.csv
or method file assignment. 

But then, otherwise not really.
instrument is important for the path generations right?


Others are indeed essential for the core genertor functionality e.g. sampler.toml or qc_layouts, que_patterns.



