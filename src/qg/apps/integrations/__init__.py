"""Swappable sample-source / queue-sink integrations for the queue apps.

The ``local_*`` modules are B-Fabric-free (used by ``queue_app_local.py``); the
``bfabric_*`` modules wrap the FGCZ portal source/sink (used by ``queue_app.py``).
"""
