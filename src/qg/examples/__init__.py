"""Bundled, package-accessible example assets (sample tables, ...).

Authoritative source for the example sample tables shown in the local queue app.
``docs/examples/`` mirrors the files under ``sample_tables/`` for repository
browsing; a byte-equality test keeps the two in sync.
"""
